<%- include('partials/head', { title: t('games.title') }) %>
<%- include('partials/nav') %>

<main class="pt-14 max-w-3xl mx-auto px-6 py-24 text-center">
  <h1 class="text-3xl font-bold text-neutral-100 mb-4"><%= t('games.title') %></h1>
  <p class="text-2xl font-semibold text-purple-400 mb-3"><%= t('games.comingSoon') %></p>
  <p class="text-neutral-500 text-sm max-w-md mx-auto"><%= t('games.comingSoonSubtitle') %></p>
</main>

<%- include('partials/foot') %>
